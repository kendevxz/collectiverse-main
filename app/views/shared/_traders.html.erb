<div class="board">
  <div class="board__container">
    <span class="board__main__text static__text xxl__font"><strong>Top Traders</strong></span>
    <div class="line__border"></div>
    <div class="board__content">
      <% User.all.first(10).each do |user| %>
        <span class="board__content__text"><%= link_to user.username.truncate(9), user_path(user) %>
        &nbsp;<span class="static__text">·5k</span></span>
      <% end %>
    </div>
  </div>
</div>
