<div class="board">
<div class="board__container">
  <span class="board__main__text static__text xxl__font"><strong>TOY RELEASES</strong></span>
  <div class="line__border"></div>
    <div class="board__content">
      <% Toy.all.each do |toy| %>
        <span , class="board__content__text"><%= toy.name.titleize.truncate(25) %></span>
      <% end %>
    </div>
    <div class="line__border"></div>
  <%= link_to 'All Toy Releases', toys_path, class: "board__additional__button" %>
  </div>
</div>
