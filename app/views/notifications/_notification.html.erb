<div>
  <span><%= link_to post.title.titleize, post_path(post.id)%></span>
  <span>| <%= user.username %> | <%= notification.created_at.to_date %> | <%= notification.read? ? "Read" : "Unread" %> </span>
</div>
